# 회고: AI-Board 통합 (session_board) — 2026-07-06 ~ 2026-07-08

## 요약
- 태스크 10개 (Done 8 / Review 2 / Rejected 0)
- 1차 PASS율: 87.5% (8건 중 7건 1차 PASS, 1건 Rejected→재작업→PASS)
- 블로커: 0건
- 보드 밖 커밋: 8건 / 14건 (57%) — 단, 커밋 컨벤션 미도입 기간 포함 (아래 상세)

## 리뷰가 실제로 잡은 결함

| 태스크 | 리뷰 판정 | FAIL 사유 |
|---|---|---|
| task-Nu4jNhME (SPEC 문서) | 1차 Rejected | 사용자 반려 3건: 기존 UI 비침범 보장 부족, 화면 목업 미제공, MCP 미도달 시 미노출 구체화 부족 |
| task-cJTeQp2t (TS/shadcn 인프라) | 1차 PASS (human-verified) | — |
| task-VOOp_2aq (칸반 컴포넌트 이식) | 1차 PASS | — |
| task-46apWJUG (KanbanPage 통합) | 1차 PASS (human-verified) | — |
| task-GtkiLmXx (테스트 픽스처) | 1차 PASS | — |
| task-QY1WXQQI (web.py 폐기) | 1차 PASS (사용자 승인 후 실행) | — |
| task-O1aUOIlJ (통합 검증) | 1차 PASS | — |
| task-w51waGcE (코드 리뷰) | 1차 PASS | 비차단 Warning 4건 발견 (activityHours 클램프, sidebar 724줄, SIGTERM 훅, useTeams catch 무음) |
| task-Lld_mySw (DB 리더) | PASS but [human] 대기 | PG/Supabase 검증 불가로 Review 유지 |
| task-JJxVEf_Q (라우트 5종) | PASS but [human] 대기 | 동일 — PG timestamp Z 직렬화 검증 불가 |

**러버스탬프 평가**: 1차 PASS율 87.5%이나, FAIL 1건(SPEC 반려)은 사용자가 직접 발견한 것이고 자동 리뷰어가 잡은 결함이 아님. 자동 리뷰는 Warning 수준 발견(activityHours, sidebar 줄수)만 산출. 리뷰어의 구조적 결함 탐지 능력은 미검증 — 러버스탬프 의심 약간 있음.

## 가장 자주 깨진 규칙

1. **[human] 게이트 장기 체류**: Review 상태 2건이 PG/Supabase 환경 미확보로 Done 전환 불가. 자동 검증 불가 조건이 태스크 완료를 무기한 차단.
2. **보드 밖 커밋 다수**: 14건 중 태스크 ID가 있는 커밋은 `task-QY1WXQQI`(1598fc8), `task-GtkiLmXx`(fff48f5) = 2건. 나머지 12건은 태스크 ID 없음. 단, 이 중 8건(069c7c6~14b46dd)은 agents 제거 + 플러그인 작업으로 보드에 등록되지 않은 별도 작업. 4건(159f306~8e5c860)은 단일 세션 모델 전환 기반작업.
3. **handoff 노트 필수항목 준수율**: 작성된 handoff 4건 모두 형식 준수 (완료한 것 / 검증 결과 / 검증 못한 것 / 주의사항 / 변경 파일). 양호.
4. **QA-FAIL 노트**: 0건. QA 루프 계측이 아예 없음 — 시스템이 QA-FAIL을 기록하지 않았거나 QA 루프 자체가 발동하지 않았음. 계측 누락 가능성 있음.

## 프로세스 마찰

- **PG/Supabase [human] 게이트**: 2개 태스크가 로컬 환경에서 PG 검증 불가로 영구 Review 상태. 이 게이트를 해소하려면 Supabase env가 있는 환경에서 별도 확인이 필요한데, 워크플로우에 이를 처리하는 경로가 없음.
- **agents 제거 작업이 보드 미등록**: 7-08에 수행한 agents 제거 + 코드 품질 개선이 `/plan` 없이 대화형으로 진행됨. 결과적으로 보드에 기록이 없고 커밋만 존재.

## 튜닝 제안

1. **[human] 게이트 타임아웃 도입**: Review에 [human] 대기 중인 태스크가 N일 이상 체류하면 "검증 불가 항목은 별도 이슈로 분리, auto 조건 충족이면 Done" 정책 검토. 현재는 무기한 대기.
2. **QA-FAIL 계측 강화**: plan-run에서 QA 루프 발동 시 `QA-FAIL n회차` 노트가 자동 기록되는지 확인. 현재 0건은 계측 누락 가능성.
3. **보드 밖 작업 경량 경로**: agents 제거처럼 `/plan` 없이 진행하는 작업을 사후에라도 보드에 기록하는 `/quick` 경로 고려.

## 보드 밖 커밋 상세

| 커밋 | 태스크 ID | 분류 |
|---|---|---|
| 14b46dd migration fix | 없음 | 우회 (agents 제거 후속) |
| ba182b2 docs 도구목록 | 없음 | 우회 (플러그인 배포 작업) |
| e709761 docs 재설치 | 없음 | 우회 |
| 069c7c6 agents 제거 | 없음 | 우회 (주요 리팩터링) |
| ac3b155 marketplace fix | 없음 | 우회 (플러그인 작업) |
| 9a6a172 plugin 번들 | 없음 | 우회 |
| 7720788 commands 이동 | 없음 | 우회 |
| 6c83b43 설계문서 삭제 | 없음 | 우회 |
| bba86e4 문서 재조정 | 없음 | 우회 |
| 159f306 단일세션 전환 | 없음 | 우회 (기반 리팩터링) |
| 8e5c860 gitignore | 없음 | 정당한 예외 (설정) |
| 2f04240 워크플로우 완성 | 없음 | 정당한 예외 (인프라) |
| fff48f5 테스트 픽스처 | task-GtkiLmXx | 보드 등록 |
| 1598fc8 web.py 폐기 | task-QY1WXQQI | 보드 등록 |

**기준선 부기**: 태스크 ID 커밋 컨벤션이 이 프로젝트에서 아직 공식 도입되지 않음. 위 "우회" 분류는 참고용이며, 컨벤션 도입 후 다음 회고에서 의미 있는 비율 측정 가능.
